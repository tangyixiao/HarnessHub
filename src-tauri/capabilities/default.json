{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Harness Hub 主窗口的默认能力集。只开放核心权限：不授予文件系统、Shell、HTTP 等插件权限，需要时逐条显式添加并在 ADR 中记录理由。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
