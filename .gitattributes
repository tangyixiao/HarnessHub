# 仓库内统一使用 LF；Windows 专用脚本保留 CRLF。
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不做行尾转换
*.png binary
*.ico binary
*.icns binary
*.sqlite3 binary

# 锁文件在 diff 中折叠，但不忽略（必须提交）
pnpm-lock.yaml -diff
Cargo.lock -diff
uv.lock -diff
