# Harness Hub v0.1 Walking Skeleton 收口 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 v0.1 的最细链路端到端跑通 —— Codex 检测 → PTY 启动 → Session 落库 → ccusage 导入 → Dashboard 真实数字 → 退出重启后历史仍在。

**Architecture:** 在已就位的骨架上（Tauri 2 + React + SQLite 迁移 + 模块骨架），按「纯逻辑先行、IO 后置」推进：每个 Task 先用注入式依赖（Fake）把可测逻辑与接口做出来，再实现系统 IO 版本，最后接线到 IPC 与 UI。`HarnessAdapter`（生命周期）与 `UsageAdapter`（数据）始终保持分离（ADR-0002）。

**Tech Stack:** Rust 2021（tauri 2、rusqlite 0.40 + bundled SQLite、thiserror 2、uuid 1、portable-pty）/ React 19 + TypeScript 6 + Vite 8 + Tailwind 4 / Vitest 5 + Testing Library / Python sidecar（零第三方依赖）

**Spec:** [HARNESS_HUB_PROJECT_PLAN.md](../../HARNESS_HUB_PROJECT_PLAN.md) —— §3.1 必做、§5 Adapter 设计、§6 统一数据模型、§8 仓库结构、§9 Phase 1、§20 v0.1 开发顺序、§21 Release Gate

## Global Constraints

以下约束对**每个 Task** 都成立，不再逐条重复：

1. **Local-first**：原始 Prompt / Response / Code / Session 数据默认不上传；网络能力必须显式、可关闭、可解释；估算值必须标记 `estimated`。
2. **外部 Harness 数据只读**：只读取其目录与日志，绝不写入、绝不修改。
3. **迁移只增不改**：schema 变更必须新增 `src-tauri/src/db/migrations/NNNN_*.sql`，禁止修改已提交的 migration。
4. **幂等由数据库唯一约束保证**：`usage_events.dedupe_key` 唯一、`sessions(harness_id, source_session_id)` 部分唯一。应用层不得用「先查再插」代替约束。
5. **`runtime_target_id` 必填**：所有 Session 都必须关联运行目标；v0.1 只有 `local`（ADR-0021）。
6. **分层**：领域模块（db / harness / usage / session / pty / …）不得依赖 Tauri GUI 类型；只有 `commands.rs` 与 `lib.rs::run()` 接触 Tauri。
7. **前端 IPC 单入口**：`src/features/*` 组件禁止直接 `invoke()`，必须走 `src/lib/ipc.ts`。
8. **未验证不宣称**：能力矩阵里为 `true` 的项必须有对应测试或验收记录；否则保持 `false`，并把理由写进代码注释。
9. **提交前全绿**：`pnpm verify` 与 `pnpm python:test` 必须通过，提交信息用 `feat:` / `fix:` / `test:` / `docs:` 前缀，一次提交只做一件事。
10. **TDD**：先写失败测试 → 确认失败 → 最小实现 → 确认通过 → 重构 → 提交。

## 当前基线（已完成，勿重复实现）

| 已存在 | 位置 | 状态 |
| --- | --- | --- |
| Tauri 2 应用壳 + CSP | `src-tauri/tauri.conf.json`、`src-tauri/src/lib.rs` | `cargo test` 通过 |
| React 应用壳 + 7 个页面路由 | `src/app/`、`src/features/` | 13 个 vitest 用例通过 |
| IPC 单入口 + 降级语义 | `src/lib/ipc.ts` | 9 个用例通过 |
| SQLite 连接 + 迁移执行器 + `0001_init` | `src-tauri/src/db/` | 迁移与约束用例通过 |
| `HarnessAdapter` / `HarnessCapabilities` / `HarnessRegistry` | `src-tauri/src/harness/` | 注册表用例通过 |
| `UsageAdapter` / `UsageTotals` | `src-tauri/src/usage/` | 汇总用例通过 |
| `SessionStore` | `src-tauri/src/session/store.rs` | 增删查 + 幂等用例通过 |
| Python sidecar stdio JSON-RPC | `python/harness_hub_runtime/rpc/` | 12 个用例通过 |
| 占位模块（不提供公开 API） | `git` / `process` / `pty` / `watcher` / `sidecar` / `trace` / `permissions` / `plugins` / `runtime` / `hhar` / `eval` | 仅文档 |

**本机实测事实（写实现时以此为准）：**

- `~/.codex/` 存在，内含 `sessions/YYYY/MM/DD/rollout-*.jsonl`、`history.jsonl`、`session_index.jsonl`、`version.json`、`config.toml`。
- rollout JSONL 单行字段为 `{ ordinal, payload, timestamp, type }`。
- `version.json` 的 `latest_version` 是**上游最新可用版本**，不是本机已安装版本 —— 不得当作 installed version。
- `codex` / `claude` / `gemini` 由 npm 全局安装，同时存在 `codex`（bash shim）、`codex.cmd`、`codex.ps1`；`opencode` 是 Scoop 的 `.exe`。
- 本机 `PATHEXT` **不包含 `.PS1`** —— 只按 PATHEXT 查找会漏掉只有 `.ps1` 的情况。
- 本机**未安装 ccusage** —— 导入路径必须能优雅处理「工具缺失」。

---

## File Structure

```text
src-tauri/src/
├── clock.rs                      新建：无依赖 RFC3339 时间格式化（Task 3）
├── harness/
│   ├── probe.rs                  新建：HostProbe trait + SystemHostProbe（Task 1）
│   ├── adapters/codex.rs         新建：Codex 适配器（Task 1）+ PTY 启动接线（Task 4）
│   └── registry.rs               修改：新增 summaries()（Task 2）
├── runtime/local.rs              新建：local runtime target 引导（Task 3）
├── session/service.rs            新建：SessionService 编排（Task 3）
├── pty/
│   ├── backend.rs                新建：PtyBackend trait + 启动程序解析（Task 4）
│   ├── session.rs                新建：PtySession 注册表（Task 4）
│   └── portable_pty_backend.rs   新建：真实 PTY 实现（Task 4）
├── usage/
│   ├── ccusage.rs                新建：ccusage JSON 模型 + 归一化（Task 5）
│   └── importer.rs               新建：入库 + 幂等统计（Task 5）
└── commands.rs                   修改：逐个 Task 增加 IPC 命令

src/
├── lib/ipc.ts                    修改：增加命令包装（Task 2 / 3 / 5 / 6）
└── features/
    ├── harnesses/HarnessesPage.tsx   修改：真实能力矩阵（Task 2）
    ├── sessions/SessionsPage.tsx     修改：真实 Session 列表（Task 3）
    └── dashboard/DashboardPage.tsx   修改：真实 Token 数字（Task 6）

fixtures/ccusage/daily.json       新建：真实 ccusage 输出夹具（脱敏）（Task 5）
```

---

### Task 1: Codex 二进制探测与版本解析

**Files:**
- Create: `src-tauri/src/harness/probe.rs`
- Create: `src-tauri/src/harness/adapters/codex.rs`
- Modify: `src-tauri/src/harness/mod.rs`（增加 `pub mod probe;` 与 re-export）
- Modify: `src-tauri/src/harness/adapters/mod.rs`（增加 `pub mod codex;`）
- Test: 两个新文件内的 `#[cfg(test)] mod tests`

**Interfaces:**
- Consumes: `crate::error::Result`、`crate::harness::adapter::{DetectResult, HarnessAdapter, HarnessCapabilities, HarnessId, LaunchRequest, ProcessHandle, ResumeRequest}`
- Produces:
  - `trait HostProbe: Send + Sync { fn find_executable(&self, name: &str) -> Option<PathBuf>; fn read_version(&self, executable: &Path) -> Result<Option<String>>; fn dir_exists(&self, path: &Path) -> bool; fn home_dir(&self) -> Option<PathBuf>; }`
  - `struct SystemHostProbe`（`SystemHostProbe::new()`）
  - `fn candidate_paths(dir: &Path, name: &str) -> Vec<PathBuf>`
  - `fn parse_version(raw: &str) -> Option<String>`
  - `const CODEX_ID: &str = "codex"`、`struct CodexAdapter::new(Arc<dyn HostProbe>)`
  - `fn codex_data_dir_candidates(home: &Path) -> Vec<PathBuf>`

- [ ] **Step 1: 写失败的测试（`probe.rs` 的纯函数）**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    #[test]
    fn candidate_paths_puts_real_windows_executables_before_the_bash_shim() {
        let dir = PathBuf::from("D:/npm-global");
        let candidates = candidate_paths(&dir, "codex");

        if cfg!(windows) {
            // 回归锁：无扩展名的 `codex` 是 POSIX bash 脚本，Windows 跑不了。
            // 真机验证时正是它被优先命中，导致版本永远读不出来（UI 显示「版本未知」）。
            let cmd_index = candidates
                .iter()
                .position(|path| path == &dir.join("codex.cmd"))
                .expect("Windows 上应有 codex.cmd 候选");
            let bare_index = candidates
                .iter()
                .position(|path| path == &dir.join("codex"))
                .expect("应保留无扩展名兜底");

            assert!(cmd_index < bare_index, "Windows 上 .cmd 必须先于无扩展名 bash shim");
            assert_eq!(candidates.last(), Some(&dir.join("codex")), "无扩展名只能最后兜底");
            assert!(candidates.contains(&dir.join("codex.ps1")), "PATHEXT 不含 .PS1，必须显式覆盖");
        } else {
            assert_eq!(candidates[0], dir.join("codex"), "非 Windows 上无扩展名优先");
        }
    }

    #[test]
    fn parse_version_extracts_semver_from_cli_output() {
        assert_eq!(parse_version("codex-cli 0.152.1"), Some("0.152.1".to_string()));
        assert_eq!(parse_version("v1.2.3"), Some("1.2.3".to_string()));
        assert_eq!(parse_version("codex 0.152.1\n"), Some("0.152.1".to_string()));
        assert_eq!(parse_version("codex-cli 1.2"), Some("1.2".to_string()));
    }

    #[test]
    fn parse_version_returns_none_when_no_version_present() {
        assert_eq!(parse_version(""), None);
        assert_eq!(parse_version("codex-cli"), None);
        assert_eq!(parse_version("no digits here"), None);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness::probe`
Expected: FAIL —— `cannot find function candidate_paths` / `parse_version`

- [ ] **Step 3: 实现 `probe.rs`**

```rust
//! 外部 Harness 的二进制与版本探测。
//!
//! Windows 现实（本机实测）：npm 全局安装的 Harness 是 **shim 三件套** ——
//! `codex`（POSIX bash 脚本）、`codex.cmd`、`codex.ps1` 并存，而 `PATHEXT` **不含 `.PS1`**。
//!
//! 两条硬规则：
//!   1. 无扩展名的 `codex` 是 shell 脚本，Windows **无法** CreateProcess，只能作最后兜底。
//!      （真机踩过：优先返回它导致 `--version` 永远读不出来。）
//!   2. 必须显式覆盖 `.ps1`，否则「只有 ps1 的机器」会被误判为未安装。

use std::collections::HashSet;
use std::path::{Path, PathBuf};
use std::process::Command;

use crate::error::Result;

#[cfg(windows)]
const EXECUTABLE_EXTENSIONS: &[&str] = &["cmd", "exe", "bat", "com", "ps1"];

#[cfg(not(windows))]
const EXECUTABLE_EXTENSIONS: &[&str] = &[];

/// 给定目录下的候选可执行路径，顺序即优先级。
///
/// Windows：先枚举真实可执行扩展名，无扩展名只作最后兜底。
/// 其他平台：无扩展名优先。
pub fn candidate_paths(dir: &Path, name: &str) -> Vec<PathBuf> {
    let mut candidates: Vec<PathBuf> = EXECUTABLE_EXTENSIONS
        .iter()
        .map(|extension| dir.join(format!("{name}.{extension}")))
        .collect();

    candidates.push(dir.join(name));
    candidates
}

/// 从 `--version` 输出中抽取第一个形如 `1.2` / `1.2.3` 的 token。
pub fn parse_version(raw: &str) -> Option<String> {
    raw.split_whitespace()
        .map(|token| token.trim_start_matches('v'))
        .find(|token| {
            let mut parts = token.split('.');
            match (parts.next(), parts.next()) {
                (Some(major), Some(minor)) => {
                    !major.is_empty()
                        && !minor.is_empty()
                        && major.chars().all(|c| c.is_ascii_digit())
                        && minor.chars().all(|c| c.is_ascii_digit())
                }
                _ => false,
            }
        })
        .map(str::to_string)
}

/// 探测外部 Harness 所需的宿主能力。
///
/// 抽成 trait 是为了让检测逻辑可以脱离真实文件系统与真实进程测试。
pub trait HostProbe: Send + Sync {
    fn find_executable(&self, name: &str) -> Option<PathBuf>;
    fn read_version(&self, executable: &Path) -> Result<Option<String>>;
    fn dir_exists(&self, path: &Path) -> bool;
    fn home_dir(&self) -> Option<PathBuf>;
}

/// 真实宿主实现。
pub struct SystemHostProbe;

impl SystemHostProbe {
    pub fn new() -> Self {
        Self
    }

    fn path_entries() -> Vec<PathBuf> {
        std::env::var_os("PATH")
            .map(|value| std::env::split_paths(&value).collect())
            .unwrap_or_default()
    }
}

impl Default for SystemHostProbe {
    fn default() -> Self {
        Self::new()
    }
}

impl HostProbe for SystemHostProbe {
    fn find_executable(&self, name: &str) -> Option<PathBuf> {
        let mut seen = HashSet::new();
        for dir in Self::path_entries() {
            if !seen.insert(dir.clone()) {
                continue;
            }
            for candidate in candidate_paths(&dir, name) {
                if candidate.is_file() {
                    return Some(candidate);
                }
            }
        }
        None
    }

    fn read_version(&self, executable: &Path) -> Result<Option<String>> {
        let output = Command::new(executable).arg("--version").output()?;
        if !output.status.success() {
            return Ok(None);
        }
        let stdout = String::from_utf8_lossy(&output.stdout);
        Ok(parse_version(&stdout))
    }

    fn dir_exists(&self, path: &Path) -> bool {
        path.is_dir()
    }

    fn home_dir(&self) -> Option<PathBuf> {
        std::env::var_os("USERPROFILE")
            .or_else(|| std::env::var_os("HOME"))
            .map(PathBuf::from)
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness::probe`
Expected: PASS（3 个用例）

- [ ] **Step 5: 写 CodexAdapter 的失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::harness::adapter::HarnessAdapter;
    use std::collections::{HashMap, HashSet};

    /// 可编程的假宿主：不碰真实文件系统与真实进程。
    struct FakeHostProbe {
        executables: HashMap<String, PathBuf>,
        versions: HashMap<PathBuf, String>,
        dirs: HashSet<PathBuf>,
        home: Option<PathBuf>,
    }

    impl FakeHostProbe {
        fn new() -> Self {
            Self {
                executables: HashMap::new(),
                versions: HashMap::new(),
                dirs: HashSet::new(),
                home: Some(PathBuf::from("/home/dev")),
            }
        }

        fn with_binary(mut self, name: &str, path: &str, version: &str) -> Self {
            let path = PathBuf::from(path);
            self.executables.insert(name.to_string(), path.clone());
            self.versions.insert(path, version.to_string());
            self
        }

        fn with_dir(mut self, path: &str) -> Self {
            self.dirs.insert(PathBuf::from(path));
            self
        }
    }

    impl HostProbe for FakeHostProbe {
        fn find_executable(&self, name: &str) -> Option<PathBuf> {
            self.executables.get(name).cloned()
        }

        fn read_version(&self, executable: &Path) -> Result<Option<String>> {
            Ok(self.versions.get(executable).and_then(|raw| parse_version(raw)))
        }

        fn dir_exists(&self, path: &Path) -> bool {
            self.dirs.contains(path)
        }

        fn home_dir(&self) -> Option<PathBuf> {
            self.home.clone()
        }
    }

    fn adapter(probe: FakeHostProbe) -> CodexAdapter {
        CodexAdapter::new(std::sync::Arc::new(probe))
    }

    #[test]
    fn reports_not_installed_when_binary_missing() {
        let result = adapter(FakeHostProbe::new()).detect();

        assert!(!result.installed);
        assert!(result.binary_path.is_none());
        assert!(result.version.is_none());
    }

    #[test]
    fn reports_binary_path_and_parsed_version_when_installed() {
        let probe = FakeHostProbe::new().with_binary("codex", "D:/npm-global/codex.cmd", "codex-cli 0.152.1");

        let result = adapter(probe).detect();

        assert!(result.installed);
        assert_eq!(result.binary_path.as_deref(), Some("D:/npm-global/codex.cmd"));
        assert_eq!(result.version.as_deref(), Some("0.152.1"));
    }

    #[test]
    fn only_reports_data_dirs_that_actually_exist() {
        let probe = FakeHostProbe::new()
            .with_binary("codex", "D:/npm-global/codex.cmd", "codex-cli 0.152.1")
            .with_dir("/home/dev/.codex");

        let result = adapter(probe).detect();

        // 必须用 PathBuf 构造期望值：Windows 上 join 产生 `\`，
        // 硬编码 `/` 会让这条测试在 Windows 上误报失败（实测踩过）。
        let expected = PathBuf::from("/home/dev").join(".codex");
        assert_eq!(
            result.data_paths,
            vec![expected.to_string_lossy().into_owned()]
        );
    }

    #[test]
    fn stays_installed_even_when_version_read_fails() {
        let probe = FakeHostProbe::new().with_binary("codex", "D:/npm-global/codex.cmd", "");

        let result = adapter(probe).detect();

        assert!(result.installed, "读不到版本不等于没安装");
        assert!(result.version.is_none());
    }

    #[test]
    fn no_capability_is_claimed_before_its_task_lands() {
        let capabilities = adapter(FakeHostProbe::new()).capabilities();

        assert_eq!(
            capabilities,
            HarnessCapabilities::default(),
            "PTY / usage / replay 都还没实现，任何能力为 true 都是不诚实的宣称"
        );
    }

    #[test]
    fn launch_reports_a_clear_error_before_pty_wiring() {
        let request = LaunchRequest {
            project_id: None,
            cwd: "D:/work".to_string(),
            args: Vec::new(),
            runtime_target_id: "local".to_string(),
        };

        let error = adapter(FakeHostProbe::new()).launch(request).expect_err("Task 4 之前必须失败");

        assert!(error.to_string().contains("PTY"), "错误信息要指出缺失的能力：{error}");
    }
}
```

- [ ] **Step 6: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness::adapters::codex`
Expected: FAIL —— `cannot find type CodexAdapter`

- [ ] **Step 7: 实现 `codex.rs`**

```rust
//! Codex CLI 适配器。
//!
//! 只实现「检测 + 能力声明 + 启动」中的检测与能力；日志解析属于 `usage`（ADR-0002）。

use std::path::{Path, PathBuf};
use std::sync::Arc;

use crate::error::{Error, Result};
use crate::harness::adapter::{
    DetectResult, HarnessAdapter, HarnessCapabilities, HarnessId, LaunchRequest, ProcessHandle,
    ResumeRequest,
};
use crate::harness::probe::HostProbe;

pub const CODEX_ID: &str = "codex";
const EXECUTABLE_NAME: &str = "codex";

/// Codex 的数据目录候选：Windows 与 Linux 一致，都是 `~/.codex`。
pub fn codex_data_dir_candidates(home: &Path) -> Vec<PathBuf> {
    vec![home.join(".codex")]
}

pub struct CodexAdapter {
    probe: Arc<dyn HostProbe>,
}

impl CodexAdapter {
    pub fn new(probe: Arc<dyn HostProbe>) -> Self {
        Self { probe }
    }

    fn existing_data_dirs(&self) -> Vec<String> {
        let Some(home) = self.probe.home_dir() else {
            return Vec::new();
        };
        codex_data_dir_candidates(&home)
            .into_iter()
            .filter(|path| self.probe.dir_exists(path))
            .map(|path| path.to_string_lossy().into_owned())
            .collect()
    }
}

impl HarnessAdapter for CodexAdapter {
    fn id(&self) -> HarnessId {
        HarnessId::from(CODEX_ID)
    }

    fn detect(&self) -> DetectResult {
        let binary = self.probe.find_executable(EXECUTABLE_NAME);
        let version = binary
            .as_deref()
            .and_then(|path| self.probe.read_version(path).ok().flatten());

        DetectResult {
            installed: binary.is_some(),
            binary_path: binary.map(|path| path.to_string_lossy().into_owned()),
            version,
            data_paths: self.existing_data_dirs(),
        }
    }

    fn version(&self) -> Result<Option<String>> {
        match self.probe.find_executable(EXECUTABLE_NAME) {
            Some(path) => self.probe.read_version(&path),
            None => Ok(None),
        }
    }

    /// **全部为 `false`**，这是当前唯一诚实的取值。
    ///
    /// Global Constraint #8 / ADR-0022：能力为 `true` 必须有测试或验收记录支撑。
    /// PTY（Task 4）与 ccusage（Task 5）都还没落地，因此 launch / terminal / resume /
    /// usage 一律不得宣称支持。每实现一项，就在对应 Task 验收通过后单独置 `true`，
    /// 不要提前把整组打开。
    fn capabilities(&self) -> HarnessCapabilities {
        HarnessCapabilities::default()
    }

    fn launch(&self, _request: LaunchRequest) -> Result<ProcessHandle> {
        Err(Error::InvalidInput(
            "Codex 尚未接入 PTY 启动（见 docs/plans Task 4）".to_string(),
        ))
    }

    fn resume(&self, _request: ResumeRequest) -> Result<ProcessHandle> {
        Err(Error::InvalidInput(
            "Codex resume 尚未接入（见 docs/plans Task 4）".to_string(),
        ))
    }
}
```

- [ ] **Step 8: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness`
Expected: PASS（probe 3 个 + codex 6 个 + registry 5 个）

- [ ] **Step 9: 提交**

```bash
git add src-tauri/src/harness/probe.rs src-tauri/src/harness/mod.rs \
        src-tauri/src/harness/adapters/mod.rs src-tauri/src/harness/adapters/codex.rs
git commit -m "feat: detect Codex binary, version and existing data dirs"
```

---

### Task 2: 能力矩阵接线到注册表、IPC 与 Harnesses 页面

**Files:**
- Modify: `src-tauri/src/harness/registry.rs`（新增 `HarnessSummary` + `summaries()`）
- Modify: `src-tauri/src/harness/mod.rs`（re-export `HarnessSummary`）
- Modify: `src-tauri/src/lib.rs`（`AppState` 增加 `harnesses`，启动时注册 Codex）
- Modify: `src-tauri/src/commands.rs`（新增 `list_harnesses`）
- Modify: `src/lib/ipc.ts`（新增 `listHarnesses()` 与类型）
- Modify: `src/features/harnesses/HarnessesPage.tsx`
- Create: `src/features/harnesses/HarnessesPage.test.tsx`

**Interfaces:**
- Consumes: Task 1 的 `CodexAdapter` / `SystemHostProbe`；既有 `HarnessRegistry`
- Produces:
  - `#[derive(Serialize)] pub struct HarnessSummary { pub id: String, pub display_name: String, pub installed: bool, pub binary_path: Option<String>, pub version: Option<String>, pub capabilities: HarnessCapabilities, pub data_paths: Vec<String> }`（`#[serde(rename_all = "camelCase")]`）
  - `HarnessRegistry::summaries(&self) -> Vec<HarnessSummary>`
  - IPC 命令 `list_harnesses() -> Vec<HarnessSummary>`
  - 前端 `listHarnesses(): Promise<IpcResult<HarnessSummary[]>>`、`normalizeHarnessSummary(raw: unknown): HarnessSummary | null`

> **契约前置条件（执行本 Task 前必须先改）**：`HarnessCapabilities` 目前的 serde 是
> `rename_all = "snake_case"`，序列化出 `tool_calls` / `live_state`；而前端类型读的是
> `toolCalls` / `liveState`。两者不一致会让能力矩阵**静默全为 false**。
> 因此 Step 3 之前先把 `src-tauri/src/harness/adapter.rs` 的该属性改为 `"camelCase"`
> （与 `binaryPath` / `dataPaths` / `displayName` 等其余 IPC 载荷一致），并用一条序列化测试锁住：

```rust
#[test]
fn capabilities_serialize_as_camel_case_for_ipc() {
    let capabilities = HarnessCapabilities { launch: true, tool_calls: true, ..Default::default() };

    // HarnessCapabilities 是 Copy，按值传入（clippy: needless_borrows_for_generic_args）。
    let json = serde_json::to_value(capabilities).expect("序列化");

    assert_eq!(json["toolCalls"], true, "IPC 契约是 camelCase");
    assert!(json.get("tool_calls").is_none(), "不得输出 snake_case 键");
}
```

> **IPC 边界必须做运行时校验**：`list_harnesses` 返回的 JSON 属于进程外数据，
> 字段改名、缺失或类型变化都不应让页面白屏。前端用一个 `normalizeHarnessSummary`
> 把未知对象收敛成安全的 `HarnessSummary`（拿到 `id` 才保留该条目，其余补默认值，
> capabilities 只有明确的 `true` 才算支持），并由 vitest 覆盖「字段残缺」「非数组」
> 「capabilities 非对象」等异常载荷。

- [ ] **Step 1: 写失败的 Rust 测试（`registry.rs`）**

```rust
    #[test]
    fn summaries_describe_every_registered_adapter() {
        let mut registry = HarnessRegistry::new();
        registry.register(Box::new(FakeAdapter::new("codex", true)));

        let summaries = registry.summaries();

        assert_eq!(summaries.len(), 1);
        assert_eq!(summaries[0].id, "codex");
        assert_eq!(summaries[0].display_name, "Codex");
        assert!(summaries[0].installed);
        assert!(summaries[0].binary_path.is_some());
        assert!(summaries[0].capabilities.launch);
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness::registry`
Expected: FAIL —— `no method named summaries`

- [ ] **Step 3: 实现 `HarnessSummary` 与 `summaries()`**

在 `registry.rs` 顶部加：

```rust
use serde::Serialize;
use crate::harness::adapter::DetectResult;

/// 一个 Harness 的对外快照：检测结果 + 能力矩阵。
#[derive(Debug, Clone, PartialEq, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct HarnessSummary {
    pub id: String,
    pub display_name: String,
    pub installed: bool,
    pub binary_path: Option<String>,
    pub version: Option<String>,
    pub capabilities: HarnessCapabilities,
    pub data_paths: Vec<String>,
}
```

在 `impl HarnessRegistry` 中加：

```rust
    /// 面向 UI 的快照。display_name 暂用 id（Task 2 只注册 Codex，
    /// 展示名在 Task 11 引入多 Harness 时移到适配器上）。
    pub fn summaries(&self) -> Vec<HarnessSummary> {
        self.iter()
            .map(|adapter| {
                let id = adapter.id();
                let detect: DetectResult = adapter.detect();
                HarnessSummary {
                    display_name: display_name_for(id.as_str()),
                    id: id.as_str().to_string(),
                    installed: detect.installed,
                    binary_path: detect.binary_path,
                    version: detect.version,
                    capabilities: adapter.capabilities(),
                    data_paths: detect.data_paths,
                }
            })
            .collect()
    }
```

并在同文件加纯函数（可测）：

```rust
/// 展示名映射。未登记的 Harness 直接回显 id，避免编造名字。
fn display_name_for(id: &str) -> String {
    match id {
        "codex" => "Codex".to_string(),
        "claude-code" => "Claude Code".to_string(),
        "gemini-cli" => "Gemini CLI".to_string(),
        "opencode" => "OpenCode".to_string(),
        other => other.to_string(),
    }
}
```

同时给 `registry.rs` 的测试模块补一条：

```rust
    #[test]
    fn display_name_falls_back_to_id_for_unknown_harness() {
        assert_eq!(display_name_for("codex"), "Codex");
        assert_eq!(display_name_for("mystery-cli"), "mystery-cli");
    }
```

- [ ] **Step 4: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml harness::registry`
Expected: PASS

- [ ] **Step 5: 接线到 `lib.rs` 与 `commands.rs`**

`lib.rs` 的 `AppState` 增加字段，并在 `setup` 中注册 Codex：

```rust
pub struct AppState {
    pub db: Mutex<Database>,
    pub harnesses: HarnessRegistry,
}
```

```rust
            let mut harnesses = HarnessRegistry::new();
            harnesses.register(Box::new(CodexAdapter::new(Arc::new(SystemHostProbe::new()))));
```

`commands.rs` 增加：

```rust
/// 已注册 Harness 的检测结果与能力矩阵。
#[tauri::command]
pub fn list_harnesses(state: State<'_, AppState>) -> Vec<HarnessSummary> {
    state.harnesses.summaries()
}
```

并注册进 `lib.rs` 的 `generate_handler![...]`。

- [ ] **Step 6: 写失败的前端测试**

```tsx
import { render, screen } from '@testing-library/react';
import { afterEach, describe, expect, it, vi } from 'vitest';

import { HarnessesPage } from '@/features/harnesses/HarnessesPage';

type Internals = { __TAURI_INTERNALS__?: unknown };

afterEach(() => {
  delete (window as unknown as Internals).__TAURI_INTERNALS__;
  vi.restoreAllMocks();
});

function stubHarnesses(payload: unknown) {
  (window as unknown as Internals).__TAURI_INTERNALS__ = {
    invoke: vi.fn(async () => payload),
  };
}

describe('HarnessesPage', () => {
  it('渲染检测到的 Harness 与能力矩阵', async () => {
    stubHarnesses([
      {
        id: 'codex',
        displayName: 'Codex',
        installed: true,
        binaryPath: 'D:/npm-global/codex.cmd',
        version: '0.152.1',
        capabilities: { launch: true, terminal: true, usage: false },
        dataPaths: ['C:/Users/dev/.codex'],
      },
    ]);

    render(<HarnessesPage />);

    expect(await screen.findByText('Codex')).toBeInTheDocument();
    expect(screen.getByText('0.152.1')).toBeInTheDocument();
    expect(screen.getByText('D:/npm-global/codex.cmd')).toBeInTheDocument();
    expect(screen.getByTestId('capability-usage')).toHaveTextContent('—');
  });

  it('浏览器模式下如实说明 IPC 不可用', async () => {
    render(<HarnessesPage />);

    expect(await screen.findByText(/请用 pnpm tauri dev 启动桌面应用/)).toBeInTheDocument();
  });

  it('没有检测到任何 Harness 时给出明确空态', async () => {
    stubHarnesses([]);

    render(<HarnessesPage />);

    expect(await screen.findByText(/未检测到任何 Harness/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 7: 运行确认失败**

Run: `pnpm test -- src/features/harnesses`
Expected: FAIL —— 找不到 `Codex` / `未检测到任何 Harness`

- [ ] **Step 8: 实现 `ipc.ts` 与 `HarnessesPage.tsx`**

`ipc.ts` 追加：

```ts
export type HarnessCapabilities = {
  launch: boolean;
  terminal: boolean;
  resume: boolean;
  usage: boolean;
  replay: boolean;
  toolCalls: boolean;
  subagents: boolean;
  liveState: boolean;
  worktree: boolean;
};

export type HarnessSummary = {
  id: string;
  displayName: string;
  installed: boolean;
  binaryPath: string | null;
  version: string | null;
  capabilities: HarnessCapabilities;
  dataPaths: string[];
};

export function normalizeHarnessSummary(raw: unknown): HarnessSummary | null {
  if (typeof raw !== 'object' || raw === null) return null;
  const source = raw as Record<string, unknown>;
  const id = typeof source.id === 'string' ? source.id.trim() : '';
  if (id.length === 0) return null;

  return {
    id,
    displayName: typeof source.displayName === 'string' && source.displayName.length > 0
      ? source.displayName
      : id,
    installed: source.installed === true,
    binaryPath: typeof source.binaryPath === 'string' ? source.binaryPath : null,
    version: typeof source.version === 'string' ? source.version : null,
    capabilities: normalizeCapabilities(source.capabilities), // 只有 === true 才算支持
    dataPaths: Array.isArray(source.dataPaths)
      ? source.dataPaths.filter((path): path is string => typeof path === 'string')
      : [],
  };
}

export async function listHarnesses(): Promise<IpcResult<HarnessSummary[]>> {
  const result = await invokeCommand<unknown>('list_harnesses');
  if (!result.ok) return result;

  const summaries = Array.isArray(result.data)
    ? result.data
        .map(normalizeHarnessSummary)
        .filter((summary): summary is HarnessSummary => summary !== null)
    : [];

  return { ok: true, data: summaries };
}
```

`HarnessesPage.tsx`：`useEffect` 调 `listHarnesses()`，四态渲染（loading / IPC 不可用 / 错误 / 就绪；就绪时空列表显示空态）。能力项用 `data-testid={`capability-${key}`}`，`true` 显示 `✓`，`false` 显示 `—`，并在页面底部说明「— 表示尚未验证支持，不等于不支持」。

页面必须覆盖的异常态（每条都要有测试）：

| 场景 | 期望 |
| --- | --- |
| 检测到且已安装 | 显示版本、binary 路径、数据目录、`已安装` |
| 已安装但版本读不到 | 显示 `版本未知`，不显示空值 |
| 未安装 | 显示 `不可用`、`未检测到可执行文件` |
| payload 字段残缺 | 不崩溃，保守显示为 `不可用` + 全部能力 `—` |
| 返回空列表 | `未检测到任何 Harness` |
| 浏览器模式 | `请用 pnpm tauri dev 启动桌面应用`，且不显示任何 Harness |
| IPC 抛错 | 显示错误原因，不白屏 |

- [ ] **Step 9: 运行确认通过**

Run: `pnpm test -- src/features/harnesses && pnpm typecheck && pnpm lint`
Expected: PASS

- [ ] **Step 10: 提交**

```bash
git add src-tauri/src/harness src-tauri/src/lib.rs src-tauri/src/commands.rs \
        src/lib/ipc.ts src/features/harnesses
git commit -m "feat: expose harness capability matrix to the UI"
```

---

### Task 3: 时间格式化、local runtime target 与 Session 编排

**Files:**
- Create: `src-tauri/src/clock.rs`
- Create: `src-tauri/src/runtime/local.rs`
- Create: `src-tauri/src/session/service.rs`
- Modify: `src-tauri/src/lib.rs`（`pub mod clock;`）
- Modify: `src-tauri/src/runtime/mod.rs`（`pub mod local;`）
- Modify: `src-tauri/src/session/mod.rs`（`pub mod service;`）
- Modify: `src-tauri/src/commands.rs`（`list_sessions`、`create_session`、`finish_session`）
- Modify: `src/lib/ipc.ts`、`src/features/sessions/SessionsPage.tsx`

**Interfaces:**
- Consumes: `SessionStore`、`NewSession`、`SessionRecord`、`Database`
- Produces:
  - `clock::format_rfc3339(unix_seconds: u64) -> String`、`clock::now_rfc3339() -> String`
  - `runtime::local::ensure_local_target(conn: &Connection) -> Result<String>`（返回 `"local"`）
  - `session::service::SessionService<'a>::new(&'a Connection)`；`start(&self, harness_id: &str, project_id: Option<&str>, cwd: Option<&str>) -> Result<SessionRecord>`；`finish(&self, hub_session_id: &str, exit_code: Option<i32>) -> Result<bool>`；`list_recent(&self, limit: u32) -> Result<Vec<SessionRecord>>`
  - IPC：`create_session(harnessId, projectId?, cwd?)`、`finish_session(hubSessionId, exitCode?)`、`list_sessions(limit?)`

- [ ] **Step 1: 写 `clock.rs` 的失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn formats_unix_epoch() {
        assert_eq!(format_rfc3339(0), "1970-01-01T00:00:00Z");
    }

    #[test]
    fn formats_known_timestamps() {
        assert_eq!(format_rfc3339(1_700_000_000), "2023-11-14T22:13:20Z");
        assert_eq!(format_rfc3339(951_782_400), "2000-02-29T00:00:00Z");
        assert_eq!(format_rfc3339(86_399), "1970-01-01T23:59:59Z");
    }

    #[test]
    fn now_is_rfc3339_shaped() {
        let now = now_rfc3339();

        assert_eq!(now.len(), 20, "期望 YYYY-MM-DDTHH:MM:SSZ，实际 {now}");
        assert!(now.ends_with('Z'));
        assert_eq!(&now[4..5], "-");
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml clock`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现 `clock.rs`**

```rust
//! 极小的 RFC3339 UTC 时间格式化。
//!
//! 刻意不引入时间库：v0.1 只需要「当前时刻 → RFC3339 字符串」，
//! 而 SQLite 侧的时间由 `strftime` 负责。

use std::time::{SystemTime, UNIX_EPOCH};

/// Unix 秒 → `YYYY-MM-DDTHH:MM:SSZ`。
pub fn format_rfc3339(unix_seconds: u64) -> String {
    let days = (unix_seconds / 86_400) as i64;
    let seconds_of_day = unix_seconds % 86_400;
    let (year, month, day) = civil_from_days(days);

    format!(
        "{year:04}-{month:02}-{day:02}T{:02}:{:02}:{:02}Z",
        seconds_of_day / 3_600,
        (seconds_of_day % 3_600) / 60,
        seconds_of_day % 60
    )
}

/// 当前时刻（UTC）。
pub fn now_rfc3339() -> String {
    let seconds = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .map(|duration| duration.as_secs())
        .unwrap_or(0);
    format_rfc3339(seconds)
}

/// Howard Hinnant `civil_from_days`：1970-01-01 起的天数 → (年, 月, 日)。
fn civil_from_days(days: i64) -> (i64, u32, u32) {
    let z = days + 719_468;
    let era = if z >= 0 { z } else { z - 146_096 } / 146_097;
    let doe = (z - era * 146_097) as u64;
    let yoe = (doe - doe / 1_460 + doe / 36_524 - doe / 146_096) / 365;
    let year = yoe as i64 + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let day = (doy - (153 * mp + 2) / 5 + 1) as u32;
    let month = if mp < 10 { mp + 3 } else { mp - 9 } as u32;

    (if month <= 2 { year + 1 } else { year }, month, day)
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml clock`
Expected: PASS

- [ ] **Step 5: 写 `runtime/local.rs` 的失败测试**

```rust
    #[test]
    fn ensure_local_target_is_idempotent() {
        let db = empty_db();

        let first = ensure_local_target(db.connection()).expect("首次引导");
        let second = ensure_local_target(db.connection()).expect("重复引导");

        assert_eq!(first, "local");
        assert_eq!(second, "local");
        let count: i64 = db
            .connection()
            .query_row("SELECT count(*) FROM runtime_targets", [], |row| row.get(0))
            .expect("计数");
        assert_eq!(count, 1, "重复引导不得插入第二行");
    }
```

- [ ] **Step 6: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml runtime::local`
Expected: FAIL

- [ ] **Step 7: 实现 `runtime/local.rs`**

```rust
//! v0.1 唯一的运行目标：本机。
//!
//! 接口为远程预留（ADR-0021），因此值放在表里而不是硬编码常量。

use rusqlite::{params, Connection};

use crate::error::Result;

/// v0.1 的 local runtime target id。
pub const LOCAL_TARGET_ID: &str = "local";

/// 确保 `local` 运行目标存在。幂等：重复调用只更新展示名与时间。
pub fn ensure_local_target(conn: &Connection) -> Result<String> {
    conn.execute(
        "INSERT INTO runtime_targets (id, kind, display_name, created_at)
         VALUES (?1, 'local', '本机', strftime('%Y-%m-%dT%H:%M:%fZ', 'now'))
         ON CONFLICT (id) DO UPDATE SET display_name = excluded.display_name",
        params![LOCAL_TARGET_ID],
    )?;
    Ok(LOCAL_TARGET_ID.to_string())
}
```

- [ ] **Step 8: 写 `session/service.rs` 的失败测试**

```rust
    #[test]
    fn start_creates_running_session_bound_to_local_target() {
        let db = seeded_db();
        ensure_local_target(db.connection()).expect("引导 runtime target");
        let service = SessionService::new(db.connection());

        let record = service
            .start("codex", None, Some("D:/work"))
            .expect("启动会话记录");

        assert_eq!(record.status, SessionStatus::Running);
        assert_eq!(record.harness_id, "codex");
        assert_eq!(record.runtime_target_id, "local");
        assert!(!record.hub_session_id.is_empty(), "必须生成 hub_session_id");
        assert!(record.started_at.ends_with('Z'));
    }

    #[test]
    fn each_start_gets_a_distinct_hub_session_id() {
        let db = seeded_db();
        ensure_local_target(db.connection()).expect("引导 runtime target");
        let service = SessionService::new(db.connection());

        let first = service.start("codex", None, None).expect("第一次");
        let second = service.start("codex", None, None).expect("第二次");

        assert_ne!(first.hub_session_id, second.hub_session_id);
        assert_eq!(service.list_recent(10).expect("列出").len(), 2);
    }

    #[test]
    fn finish_reflects_exit_code() {
        let db = seeded_db();
        ensure_local_target(db.connection()).expect("引导 runtime target");
        let service = SessionService::new(db.connection());
        let started = service.start("codex", None, None).expect("启动");

        assert!(service.finish(&started.hub_session_id, Some(0)).expect("结束"));
        assert!(!service.finish(&started.hub_session_id, Some(0)).expect("重复结束"));

        let stored = service.get(&started.hub_session_id).expect("查询").expect("应存在");
        assert_eq!(stored.status, SessionStatus::Exited);
    }

    #[test]
    fn list_recent_returns_every_started_session() {
        let db = seeded_db();
        ensure_local_target(db.connection()).expect("引导 runtime target");
        let service = SessionService::new(db.connection());
        let first = service.start("codex", None, None).expect("第一次");
        let second = service.start("codex", None, None).expect("第二次");

        let ids: Vec<String> = service
            .list_recent(10)
            .expect("列出")
            .into_iter()
            .map(|record| record.hub_session_id)
            .collect();

        // 同一秒内启动的会话相对顺序不确定（排序键是 started_at DESC, hub_session_id ASC），
        // 因此这里只断言集合内容；排序行为由 SessionStore::list_recent 的既有测试覆盖。
        assert_eq!(ids.len(), 2);
        assert!(ids.contains(&first.hub_session_id));
        assert!(ids.contains(&second.hub_session_id));
    }
```

- [ ] **Step 9: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml session::service`
Expected: FAIL

- [ ] **Step 10: 实现 `session/service.rs`**

```rust
//! Session 编排：把「谁在哪个项目、用什么 Harness、在哪运行」组装成一条库记录。

use rusqlite::Connection;
use uuid::Uuid;

use crate::clock;
use crate::error::Result;
use crate::runtime::local::LOCAL_TARGET_ID;
use crate::session::store::{NewSession, SessionRecord, SessionStore};

pub struct SessionService<'conn> {
    store: SessionStore<'conn>,
}

impl<'conn> SessionService<'conn> {
    pub fn new(conn: &'conn Connection) -> Self {
        Self {
            store: SessionStore::new(conn),
        }
    }

    /// 新建一条 `running` 会话记录，返回落库后的完整记录。
    ///
    /// `hub_session_id` 由 Harness Hub 生成（UUID v4），与外部 Harness 的
    /// `source_session_id` 完全分离。
    pub fn start(
        &self,
        harness_id: &str,
        project_id: Option<&str>,
        cwd: Option<&str>,
    ) -> Result<SessionRecord> {
        let hub_session_id = Uuid::new_v4().to_string();
        let started_at = clock::now_rfc3339();

        let mut session = NewSession::new(harness_id, harness_id, LOCAL_TARGET_ID, &started_at)
            .with_cwd(cwd.unwrap_or(""));
        session.hub_session_id = hub_session_id.clone();
        session.cwd = cwd.map(str::to_string);
        session.project_id = project_id.map(str::to_string);

        self.store.insert(&session)?;

        self.store
            .get(&hub_session_id)?
            .ok_or_else(|| crate::error::Error::InvalidInput("刚写入的会话读不回来".to_string()))
    }

    pub fn get(&self, hub_session_id: &str) -> Result<Option<SessionRecord>> {
        self.store.get(hub_session_id)
    }

    pub fn finish(&self, hub_session_id: &str, exit_code: Option<i32>) -> Result<bool> {
        self.store
            .finish(hub_session_id, exit_code, &clock::now_rfc3339())
    }

    pub fn list_recent(&self, limit: u32) -> Result<Vec<SessionRecord>> {
        self.store.list_recent(limit)
    }
}
```

> 注意 `NewSession::new` 的第一个参数是 `hub_session_id`；上面对 `cwd` 的两次赋值是冗余的，
> 执行时保留 `session.cwd = cwd.map(str::to_string);` 这一处即可，并且**必须**用
> `NewSession::new(&hub_session_id, harness_id, LOCAL_TARGET_ID, &started_at)` 直接构造，
> 避免先传 `harness_id` 当 id 再覆盖的可读性陷阱。

- [ ] **Step 11: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml session runtime clock`
Expected: PASS

- [ ] **Step 12: 接线 IPC 与 Sessions 页面**

`commands.rs` 增加三个命令（`create_session` / `finish_session` / `list_sessions`），每个都先 `ensure_local_target` 再调用 `SessionService`；`ipc.ts` 与 `SessionsPage.tsx` 按 Task 2 的同款三态模式实现；为 `SessionsPage` 增加 `SessionsPage.test.tsx`，覆盖「有会话」「无会话」「IPC 不可用」三态。

- [ ] **Step 13: 全量验证并提交**

Run: `pnpm verify && pnpm python:test`
Expected: 全部通过

```bash
git add src-tauri/src/clock.rs src-tauri/src/runtime src-tauri/src/session \
        src-tauri/src/lib.rs src-tauri/src/commands.rs src/lib/ipc.ts src/features/sessions
git commit -m "feat: persist and list harness sessions bound to a runtime target"
```

---

### Task 4: 用 PTY 真正启动 Codex

**Files:**
- Create: `src-tauri/src/pty/backend.rs`
- Create: `src-tauri/src/pty/session.rs`
- Create: `src-tauri/src/pty/portable_pty_backend.rs`
- Modify: `src-tauri/src/pty/mod.rs`
- Modify: `src-tauri/src/harness/adapters/codex.rs`（`launch` / `resume` 接 PTY）
- Modify: `src-tauri/Cargo.toml`（`cargo add portable-pty`）
- Modify: `src-tauri/src/commands.rs`、`src/lib/ipc.ts`、`src/features/terminal/TerminalPage.tsx`

**Interfaces:**
- Consumes: Task 3 的 `SessionService`、Task 1 的 `HostProbe`
- Produces:
  - `struct PtyCommand { program: PathBuf, args: Vec<String>, cwd: Option<PathBuf>, env: Vec<(String, String)>, cols: u16, rows: u16 }`
  - `fn launch_program(executable: &Path) -> (PathBuf, Vec<String>)`
  - `trait PtyBackend: Send + Sync { fn spawn(&self, session_id: &str, command: &PtyCommand) -> Result<Option<u32>>; fn write(&self, session_id: &str, bytes: &[u8]) -> Result<()>; fn resize(&self, session_id: &str, cols: u16, rows: u16) -> Result<()>; fn kill(&self, session_id: &str) -> Result<()>; fn is_running(&self, session_id: &str) -> Result<bool>; }`

- [ ] **Step 1: 写 `launch_program` 的失败测试**

```rust
    #[test]
    fn cmd_shims_are_launched_directly() {
        let (program, args) = launch_program(Path::new("D:/npm-global/codex.cmd"));

        assert_eq!(program, PathBuf::from("D:/npm-global/codex.cmd"));
        assert!(args.is_empty());
    }

    #[test]
    fn powershell_shims_are_launched_through_pwsh() {
        // 本机实测：npm 只提供 .ps1 的 Harness 无法直接 CreateProcess。
        let (program, args) = launch_program(Path::new("D:/npm-global/codex.ps1"));

        assert_eq!(program, PathBuf::from("pwsh"));
        assert_eq!(
            args,
            vec![
                "-NoLogo".to_string(),
                "-NoProfile".to_string(),
                "-File".to_string(),
                "D:/npm-global/codex.ps1".to_string(),
            ]
        );
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml pty::backend`
Expected: FAIL

- [ ] **Step 3: 实现 `pty/backend.rs`（含 `PtyCommand`、`launch_program`、`PtyBackend`）**

`launch_program` 实现即上面测试描述的行为；扩展名判断用 `eq_ignore_ascii_case("ps1")`（Windows 上大小写不敏感），shell 名称固定 `pwsh`（Windows 10+ 与 Linux 均可用 pwsh）。

- [ ] **Step 4: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml pty::backend`
Expected: PASS

- [ ] **Step 5: 拉取 PTY 依赖并确认真实 API**

```bash
cargo add --manifest-path src-tauri/Cargo.toml portable-pty
cargo doc --manifest-path src-tauri/Cargo.toml -p portable-pty --no-deps
```

把实际版本号记进 `docs/CONTEXT.md` 的技术栈表；若 `native_pty_system()` / `openpty()` / `spawn_command()` 的签名与下面代码不一致，以 `cargo doc` 输出为准调整 `portable_pty_backend.rs`（该文件是唯一与具体 PTY crate 耦合的地方）。

- [ ] **Step 6: 实现 `portable_pty_backend.rs`**

```rust
//! 真实 PTY 后端。只有这个文件依赖 `portable-pty`，其余代码只认 `PtyBackend`。

use std::collections::HashMap;
use std::io::Write;
use std::sync::Mutex;

use portable_pty::{Child, CommandBuilder, MasterPty, PtySize, native_pty_system};

use crate::error::{Error, Result};
use crate::pty::backend::{PtyBackend, PtyCommand};

struct RunningPty {
    master: Box<dyn MasterPty + Send>,
    writer: Box<dyn Write + Send>,
    child: Box<dyn Child + Send + Sync>,
}

#[derive(Default)]
pub struct PortablePtyBackend {
    sessions: Mutex<HashMap<String, RunningPty>>,
}

impl PortablePtyBackend {
    pub fn new() -> Self {
        Self::default()
    }
}

impl PtyBackend for PortablePtyBackend {
    fn spawn(&self, session_id: &str, command: &PtyCommand) -> Result<Option<u32>> {
        let pty_system = native_pty_system();
        let pair = pty_system
            .openpty(PtySize {
                rows: command.rows,
                cols: command.cols,
                pixel_width: 0,
                pixel_height: 0,
            })
            .map_err(|error| Error::InvalidInput(format!("打开 PTY 失败：{error}")))?;

        let mut builder = CommandBuilder::new(&command.program);
        builder.args(&command.args);
        if let Some(cwd) = &command.cwd {
            builder.cwd(cwd);
        }
        for (key, value) in &command.env {
            builder.env(key, value);
        }

        let child = pair
            .slave
            .spawn_command(builder)
            .map_err(|error| Error::InvalidInput(format!("启动进程失败：{error}")))?;
        // slave 端必须在这里丢弃，否则父进程持有的句柄会让子进程永不收到 EOF。
        drop(pair.slave);

        let pid = child.process_id();
        let writer = pair
            .master
            .take_writer()
            .map_err(|error| Error::InvalidInput(format!("获取 PTY writer 失败：{error}")))?;

        self.sessions.lock().map_err(|_| Error::StateLockPoisoned)?.insert(
            session_id.to_string(),
            RunningPty {
                master: pair.master,
                writer,
                child,
            },
        );

        Ok(pid)
    }

    fn write(&self, session_id: &str, bytes: &[u8]) -> Result<()> {
        let mut sessions = self.sessions.lock().map_err(|_| Error::StateLockPoisoned)?;
        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| Error::InvalidInput(format!("未知会话：{session_id}")))?;
        session
            .writer
            .write_all(bytes)
            .map_err(|error| Error::InvalidInput(format!("写入 PTY 失败：{error}")))?;
        session
            .writer
            .flush()
            .map_err(|error| Error::InvalidInput(format!("刷新 PTY 失败：{error}")))?;
        Ok(())
    }

    fn resize(&self, session_id: &str, cols: u16, rows: u16) -> Result<()> {
        let sessions = self.sessions.lock().map_err(|_| Error::StateLockPoisoned)?;
        let session = sessions
            .get(session_id)
            .ok_or_else(|| Error::InvalidInput(format!("未知会话：{session_id}")))?;
        session
            .master
            .resize(PtySize {
                rows,
                cols,
                pixel_width: 0,
                pixel_height: 0,
            })
            .map_err(|error| Error::InvalidInput(format!("调整 PTY 尺寸失败：{error}")))?;
        Ok(())
    }

    fn kill(&self, session_id: &str) -> Result<()> {
        let mut sessions = self.sessions.lock().map_err(|_| Error::StateLockPoisoned)?;
        let mut session = sessions
            .remove(session_id)
            .ok_or_else(|| Error::InvalidInput(format!("未知会话：{session_id}")))?;
        session
            .child
            .kill()
            .map_err(|error| Error::InvalidInput(format!("结束进程失败：{error}")))?;
        Ok(())
    }

    fn is_running(&self, session_id: &str) -> Result<bool> {
        let mut sessions = self.sessions.lock().map_err(|_| Error::StateLockPoisoned)?;
        let Some(session) = sessions.get_mut(session_id) else {
            return Ok(false);
        };
        Ok(session
            .child
            .try_wait()
            .map_err(|error| Error::InvalidInput(format!("查询进程状态失败：{error}")))?
            .is_none())
    }
}
```

- [ ] **Step 7: 用假后端测试编排逻辑（`pty/session.rs`）**

```rust
    #[test]
    fn spawn_records_the_session_and_calls_the_backend() {
        let backend = Arc::new(FakePtyBackend::default());
        let sessions = PtySessions::new(backend.clone());

        sessions
            .spawn("hub-1", &PtyCommand::for_binary(Path::new("D:/npm-global/codex.cmd")))
            .expect("启动");

        assert_eq!(backend.spawned(), vec!["hub-1".to_string()]);
    }

    #[test]
    fn kill_on_unknown_session_reports_a_clear_error() {
        let sessions = PtySessions::new(Arc::new(FakePtyBackend::default()));

        let error = sessions.kill("nobody").expect_err("必须失败");

        assert!(error.to_string().contains("nobody"));
    }
```

- [ ] **Step 8: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml pty`
Expected: PASS

- [ ] **Step 9: 把 `CodexAdapter::launch` 接到 PTY + Session 记录**

`launch` 流程：`SessionService::start` → 解析 binary（未安装则返回明确错误）→ `launch_program` → `PtySessions::spawn` → 更新 Session（`source_session_id` 留空，PTY 会话不需要外部 id）→ 返回 `ProcessHandle { hub_session_id, pid }`。为这条流程写一个集成测试：用假 `PtyBackend` + 内存库，断言「会话被创建 + backend 收到启动请求 + 返回的 pid 与后端一致」。

- [ ] **Step 10: 人工验收（必须留证据）**

Run: `pnpm tauri dev`
手工步骤：Harnesses 页确认 Codex 显示为「已安装」→ Sessions 页新建会话 → Terminal 页出现真实 shell 输出 → 在终端里执行 `codex --version` 能回显版本 → Kill 后 Session 状态变为 `exited`。
把实际输出粘贴进 `tests/e2e/README.md` 的验收记录（含日期与命令）。

- [ ] **Step 11: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/pty \
        src-tauri/src/harness/adapters/codex.rs src-tauri/src/commands.rs \
        src/lib/ipc.ts src/features/terminal tests/e2e
git commit -m "feat: launch Codex inside a PTY and track its session"
```

---

### Task 5: ccusage JSON 导入与幂等归一化

**Files:**
- Create: `fixtures/ccusage/daily.json`
- Create: `src-tauri/src/usage/ccusage.rs`
- Create: `src-tauri/src/usage/importer.rs`
- Modify: `src-tauri/src/usage/mod.rs`
- Modify: `src-tauri/src/commands.rs`（`import_ccusage`、`usage_summary`）
- Modify: `src-tauri/src/harness/adapters/codex.rs`（`usage` 能力置 `true`）

**Interfaces:**
- Consumes: `UsageRecord`、`UsageTotals`、`Database`
- Produces:
  - `struct CcusageDailyReport { daily: Vec<CcusageDailyEntry>, totals: Option<CcusageTotalsReport> }`（`Deserialize`，`rename_all = "camelCase"`）
  - `fn normalize_daily(harness_id: &str, report: &CcusageDailyReport) -> Vec<UsageRecord>`
  - `fn dedupe_key(harness_id: &str, day: &str, model: &str) -> String`
  - `struct UsageImporter<'a>`；`import(&self, source: &str, harness_id: &str, records: &[UsageRecord]) -> Result<ImportResult>`

- [ ] **Step 1: 采集真实夹具**

```bash
npx ccusage@latest daily --json > fixtures/ccusage/daily.json
```

若本机无法联网，则手工构造夹具，但**必须**在 `fixtures/ccusage/README.md` 注明字段来源（ccusage 官方 JSON 输出文档）与「尚未用真实输出核对」这一事实，并把该 Task 的完成标记为「部分」。脱敏要求：删除任何路径、账号、项目名。

- [ ] **Step 2: 写失败测试（解析）**

```rust
    #[test]
    fn parses_the_committed_fixture() {
        let raw = include_str!("../../../fixtures/ccusage/daily.json");
        let report: CcusageDailyReport = serde_json::from_str(raw).expect("夹具必须可解析");

        assert!(!report.daily.is_empty(), "夹具至少含一天数据");
    }

    #[test]
    fn normalizes_daily_entries_into_usage_records() {
        let raw = r#"{
            "daily": [{
                "date": "2026-09-20",
                "inputTokens": 1000,
                "outputTokens": 200,
                "cacheCreationTokens": 50,
                "cacheReadTokens": 25,
                "totalTokens": 1275,
                "totalCost": 0.42,
                "modelsUsed": ["gpt-5"],
                "modelBreakdowns": [{
                    "modelName": "gpt-5",
                    "inputTokens": 1000,
                    "outputTokens": 200,
                    "cacheCreationTokens": 50,
                    "cacheReadTokens": 25,
                    "cost": 0.42
                }]
            }]
        }"#;
        let report: CcusageDailyReport = serde_json::from_str(raw).expect("解析");

        let records = normalize_daily("codex", &report);

        assert_eq!(records.len(), 1, "一个 model breakdown 一条记录");
        let record = &records[0];
        assert_eq!(record.day, "2026-09-20");
        assert_eq!(record.model_id, "gpt-5");
        assert_eq!(record.input_tokens, 1000);
        assert_eq!(record.cache_read_tokens, 25);
        assert_eq!(record.cost_usd, Some(0.42));
        assert!(record.cost_estimated, "ccusage 成本是估算值");
        assert_eq!(record.dedupe_key, "ccusage:codex:2026-09-20:gpt-5");
    }

    #[test]
    fn normalization_is_stable_across_runs() {
        let raw = include_str!("../../../fixtures/ccusage/daily.json");
        let report: CcusageDailyReport = serde_json::from_str(raw).expect("夹具");

        let first = normalize_daily("codex", &report);
        let second = normalize_daily("codex", &report);

        assert_eq!(first, second, "同一输入必须产生同样的 dedupe_key");
    }
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml usage::ccusage`
Expected: FAIL

- [ ] **Step 4: 实现 `usage/ccusage.rs`**

```rust
//! ccusage JSON 解析与归一化。
//!
//! 决策（规格 §7）：ccusage 是 Usage Engine 第一选择，MIT License，
//! 不为每个 Harness 重写 token parser（ADR-0005 external-first）。
//!
//! 归一化是**纯函数**：给定同一份报告，永远产出同样的 `dedupe_key`，
//! 这是「import 可重复执行且不会重复计数」的前提。

use serde::Deserialize;

use crate::usage::UsageRecord;

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CcusageDailyReport {
    pub daily: Vec<CcusageDailyEntry>,
}

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CcusageDailyEntry {
    pub date: String,
    #[serde(default)]
    pub input_tokens: u64,
    #[serde(default)]
    pub output_tokens: u64,
    #[serde(default)]
    pub cache_creation_tokens: u64,
    #[serde(default)]
    pub cache_read_tokens: u64,
    #[serde(default)]
    pub total_tokens: u64,
    #[serde(default)]
    pub total_cost: f64,
    #[serde(default)]
    pub models_used: Vec<String>,
    #[serde(default)]
    pub model_breakdowns: Vec<CcusageModelBreakdown>,
}

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CcusageModelBreakdown {
    pub model_name: String,
    #[serde(default)]
    pub input_tokens: u64,
    #[serde(default)]
    pub output_tokens: u64,
    #[serde(default)]
    pub cache_creation_tokens: u64,
    #[serde(default)]
    pub cache_read_tokens: u64,
    #[serde(default)]
    pub cost: f64,
}

/// 幂等键：来源 + Harness + 天 + 模型。
pub fn dedupe_key(harness_id: &str, day: &str, model: &str) -> String {
    format!("ccusage:{harness_id}:{day}:{model}")
}

/// 把一份 ccusage daily 报告摊平成统一 Usage 记录。
pub fn normalize_daily(harness_id: &str, report: &CcusageDailyReport) -> Vec<UsageRecord> {
    report
        .daily
        .iter()
        .flat_map(|entry| {
            entry.model_breakdowns.iter().map(move |breakdown| UsageRecord {
                occurred_at: format!("{}T00:00:00Z", entry.date),
                day: entry.date.clone(),
                harness_id: harness_id.to_string(),
                model_id: breakdown.model_name.clone(),
                input_tokens: breakdown.input_tokens,
                output_tokens: breakdown.output_tokens,
                cache_creation_tokens: breakdown.cache_creation_tokens,
                cache_read_tokens: breakdown.cache_read_tokens,
                cost_usd: Some(breakdown.cost),
                // ccusage 的成本基于其内置定价表推算，属于估算值。
                cost_estimated: true,
                dedupe_key: dedupe_key(harness_id, &entry.date, &breakdown.model_name),
            })
        })
        .collect()
}
```

`CcusageDailyReport` 刻意只声明 `daily`：serde 默认忽略未知字段，因此 ccusage 报告里的 `totals`
不会导致解析失败，而我们也不需要它（总量由 `usage_summary` 从数据库聚合，避免两个真值来源）。
若后续确实需要 `totals`，再新增字段与类型，不要预先留空壳结构。

- [ ] **Step 5: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml usage::ccusage`
Expected: PASS

- [ ] **Step 6: 写失败的幂等测试（`usage/importer.rs`）**

```rust
    #[test]
    fn first_import_writes_every_record() {
        let db = seeded_db();
        let importer = UsageImporter::new(db.connection());

        let result = importer.import("ccusage", "codex", &sample_records(3)).expect("导入");

        assert_eq!(result.records_seen, 3);
        assert_eq!(result.records_imported, 3);
        assert_eq!(result.records_skipped, 0);
    }

    #[test]
    fn second_import_skips_everything_without_double_counting() {
        let db = seeded_db();
        let importer = UsageImporter::new(db.connection());
        importer.import("ccusage", "codex", &sample_records(3)).expect("首次导入");

        let result = importer.import("ccusage", "codex", &sample_records(3)).expect("重复导入");

        assert_eq!(result.records_imported, 0, "重复导入不得新增计数");
        assert_eq!(result.records_skipped, 3);
        assert_eq!(importer.total_events().expect("事件总数"), 3);
    }

    #[test]
    fn import_records_an_audit_row() {
        let db = seeded_db();
        let importer = UsageImporter::new(db.connection());
        importer.import("ccusage", "codex", &sample_records(2)).expect("导入");

        let (status, imported): (String, i64) = db
            .connection()
            .query_row("SELECT status, records_imported FROM imports LIMIT 1", [], |row| {
                Ok((row.get(0)?, row.get(1)?))
            })
            .expect("imports 审计行");

        assert_eq!(status, "succeeded");
        assert_eq!(imported, 2);
    }
```

- [ ] **Step 7: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml usage::importer`
Expected: FAIL

- [ ] **Step 8: 实现 `usage/importer.rs`**

关键点：用 `INSERT ... ON CONFLICT (dedupe_key) DO NOTHING` + `Connection::changes()` 统计导入/跳过；先插一行 `imports`（`status = 'running'`），结束时更新为 `succeeded` / `failed` 并写 `records_seen/imported/skipped`。**不要**用 `INSERT OR IGNORE`：它会吞掉外键与 NOT NULL 错误，把真实缺陷伪装成「重复数据」。

- [ ] **Step 9: 运行确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml usage`
Expected: PASS

- [ ] **Step 10: ccusage 可用性处理 + 能力升级**

`import_ccusage` 命令在找不到 ccusage 时返回明确错误（含 `npx ccusage@latest daily --json` 提示），不得静默返回 0。随后把 `CodexAdapter::capabilities().usage` 置 `true`，并同步更新 Task 1 中那条断言（改为断言 `usage` 为 `true`，注释引用本 Task 的回归测试）。

- [ ] **Step 11: 提交**

```bash
git add fixtures/ccusage src-tauri/src/usage src-tauri/src/commands.rs src-tauri/src/harness/adapters/codex.rs
git commit -m "feat: import ccusage JSON idempotently"
```

---

### Task 6: Dashboard 真实数字与重启持久化验收

**Files:**
- Modify: `src-tauri/src/commands.rs`（`usage_summary` 返回按天/模型/Harness 聚合）
- Modify: `src/lib/ipc.ts`、`src/features/dashboard/DashboardPage.tsx`
- Create: `src/features/dashboard/DashboardPage.test.tsx`
- Modify: `tests/e2e/README.md`（重启持久化验收记录）

**Interfaces:**
- Produces:
  - `#[derive(Serialize)] pub struct UsageSummary { pub total_tokens: u64, pub input_tokens: u64, pub output_tokens: u64, pub cache_read_tokens: u64, pub cache_creation_tokens: u64, pub estimated_cost_usd: Option<f64>, pub event_count: i64, pub by_model: Vec<UsageBucket>, pub by_harness: Vec<UsageBucket> }`
  - `pub struct UsageBucket { pub key: String, pub total_tokens: u64, pub estimated_cost_usd: Option<f64> }`
  - IPC `usage_summary()`

- [ ] **Step 1: 写失败的聚合测试**

```rust
    #[test]
    fn summary_sums_tokens_and_groups_by_model() {
        let db = seeded_db();
        let importer = UsageImporter::new(db.connection());
        importer.import("ccusage", "codex", &records_for_two_models()).expect("导入");

        let summary = usage_summary(&db).expect("聚合");

        assert_eq!(summary.total_tokens, 1_500);
        assert_eq!(summary.event_count, 2);
        assert_eq!(summary.by_model.len(), 2);
        assert_eq!(summary.by_model[0].key, "gpt-5");
    }

    #[test]
    fn summary_is_zero_on_empty_database() {
        let db = seeded_db();

        let summary = usage_summary(&db).expect("聚合");

        assert_eq!(summary.total_tokens, 0);
        assert_eq!(summary.event_count, 0);
        assert!(summary.by_model.is_empty());
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml usage::summary`
Expected: FAIL

- [ ] **Step 3: 实现聚合（放在 `usage/summary.rs`，SQL 聚合而非 Rust 侧遍历）**

用 `SELECT model_id, SUM(total_tokens), SUM(cost_usd) FROM usage_events GROUP BY model_id ORDER BY SUM(total_tokens) DESC`；`estimated_cost_usd` 为 `Option`：当所有事件都 `cost_estimated = 0` 时才是精确值，否则前端必须显示「估算」。

- [ ] **Step 4: 前端接线与测试**

`DashboardPage` 用 `usage_summary` 替换 Task 6 之前的占位 `—`；`DashboardPage.test.tsx` 覆盖：有数据（显示 Token 与「估算」标注）、无数据（显示 0 而不是 `—`）、IPC 不可用（沿用现有降级文案）、`estimated` 为 false 时不显示估算标注。

- [ ] **Step 5: 全量验证**

Run: `pnpm verify && pnpm python:test`
Expected: 全部通过

- [ ] **Step 6: 重启持久化人工验收（Release Gate 项）**

Run: `pnpm tauri dev` → 新建 Codex 会话 → 导入 usage → 完全退出应用 → 重新 `pnpm tauri dev`。
Expected: Sessions 与 Dashboard 数字仍在（SQLite 落盘），并在 `tests/e2e/README.md` 记录实际输出与日期。

- [ ] **Step 7: 提交**

```bash
git add src-tauri/src/usage src-tauri/src/commands.rs src/lib/ipc.ts \
        src/features/dashboard tests/e2e
git commit -m "feat: show real usage numbers on the dashboard"
```

---

## Self-Review

**1. Spec coverage（对 HARNESS_HUB_PROJECT_PLAN.md §3.1 / §9 Phase 1）**

| 规格要求 | 覆盖 Task |
| --- | --- |
| Harness Discovery（Name / Installed / Binary Path / Version / Capabilities / Data Paths） | Task 1、Task 2 |
| Unified Launcher（Project → Harness → Start，内置 Terminal） | Task 3、Task 4（Project Registry 完整化属 Task 14，不在本计划内） |
| PTY Session Manager（Launch / Kill / 多 Session / 状态持久化） | Task 4；Reconnect 属 Phase 4，不在本计划 |
| Usage Dashboard（Token / Cost / Model / Harness Breakdown、Day） | Task 5、Task 6 |
| Coding Activity History | 不在本计划（Phase 3，另立计划） |
| Git Activity | 不在本计划（Task 16，另立计划） |
| Phase 1 完成条件「退出重启后 Session 历史仍存在」 | Task 6 Step 6 |
| Release Gate「Import 可重复执行且不会重复计数」 | Task 5 Step 6 |
| Release Gate「外部日志只读」 | Global Constraints 2；Task 1 只 `is_file()` / `is_dir()` 探测 |
| Release Gate「主要 Adapter 有 fixtures」 | Task 5 Step 1（ccusage 夹具） |

**未覆盖且**有意**留在后续计划的范围**：Claude / Gemini / OpenCode 适配器（Task 11–13）、Project Registry、Activity Timeline、Git observer、Packaging、Config/Profile 平面、Python AI Runtime。它们各自独立可测，应分别立计划，不要塞进本计划。

**2. Placeholder scan**

- 已清理：Task 3 的 `list_recent` 断言改为集合断言；Task 5 的 `CcusageDailyReport` 只保留 `daily` 字段并说明理由。
- 其余步骤均含可执行代码或明确命令，无 TBD / 「稍后补充」。

**3. Type consistency**

- `HubSessionId` 全程使用 `String`（UUID v4 文本），与 `sessions.hub_session_id TEXT` 一致。
- `HarnessCapabilities` 字段名 `launch / terminal / resume / usage / replay / tool_calls / subagents / live_state / worktree` 与 `harness/adapter.rs` 一致；序列化为 camelCase 后前端类型 `HarnessCapabilities` 必须同名同形（`toolCalls` / `liveState`）。
- `UsageRecord.dedupe_key` 与 `usage_events.dedupe_key TEXT NOT NULL UNIQUE` 一致；`normalize_daily` 与 `UsageImporter` 都只通过 `dedupe_key()` 生成该值。
- IPC 命令名前后端一致：`app_info`、`db_health`、`list_harnesses`、`create_session`、`finish_session`、`list_sessions`、`import_ccusage`、`usage_summary`。
- `LOCAL_TARGET_ID = "local"` 只在一个地方定义（`runtime/local.rs`），`test_support.rs` 的 `RUNTIME_TARGET_ID` 在 Task 3 之后应改为引用该常量，避免两处硬编码漂移。
